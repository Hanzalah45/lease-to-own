# OS generated files
.DS_Store
Thumbs.db
*.log
~$*

# Environment variables
.env
.env.*
!.env.example
!.env.local.example

# Dependencies
node_modules/
# Anchored to the actual Composer vendor directory — a bare "vendor/" also
# matches backend/resources/views/vendor/ (Laravel's published, customized
# mail theme lives there and must be committed).
/backend/vendor/

# Build and cache outputs
.next/
out/
build/
dist/
.phpunit.cache/
.phpunit.result.cache
*.tsbuildinfo

# IDE files
.idea/
.vscode/
reports/~$*.docx

# Claude Code local tooling config (machine-specific paths)
.claude/
